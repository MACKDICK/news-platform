# DevOps Setup

## Docker Compose

El sistema se ejecuta completamente con Docker.

## Servicios

- Laravel API
- React Frontend
- MySQL Database

## Comando de ejecución

docker compose up --build

## Variables de entorno

DB_HOST=mysql
DB_DATABASE=news_db
DB_USERNAME=root
DB_PASSWORD=root